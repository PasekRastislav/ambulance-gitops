apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wac-hospital

labels:
- pairs:
    app.kubernetes.io/part-of: wac-hospital
    app.kubernetes.io/name: rpasek-ambulance-wl
  includeSelectors: true
  includeTemplates: true
  wac-fiit.github.io/project: rpasek-ambulance-wl

resources:
- ../../../apps/rpasek-ambulance-ufe
- ../../../apps/rpasek-ambulance-webapi

components:
- ../../../components/version-release